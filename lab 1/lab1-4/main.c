#include <stdio.h>
#include <stdlib.h>
int main( )
{
float x=0 ;
float y=0 ;
float sum=0;

printf("inter num x :");
scanf("%f" , &x);

printf("inter num y :" );
scanf("%f" , &y);

 sum=x+y;

printf("the num is : %f" , sum );
 return 0;
}
